# 📬 Notification Service

![Python](https://img.shields.io/badge/Python-3.11-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.123-green)
![RabbitMQ](https://img.shields.io/badge/RabbitMQ-3.9-orange)
![Docker](https://img.shields.io/badge/Docker-20.10-blue)
![aio-pika](https://img.shields.io/badge/aio--pika-6.8-lightgrey)
![Pydantic](https://img.shields.io/badge/Pydantic-v2-green)


Этот репозиторий содержит надёжный асинхронный микросервис уведомлений, построенный с использованием **FastAPI**, **RabbitMQ** и **Docker**. Сервис предназначен для обработки различных типов уведомлений (например, email, SMS) в надёжной и масштабируемой форме. В проекте используются очереди сообщений, фоновые воркеры и встроенный механизм повторной попытки с **Dead Letter Queue (DLQ)**.

---

## 🏗 Архитектура

Сервис состоит из нескольких компонентов, организованных через Docker Compose:

1. **FastAPI API**: Единый эндпоинт `/events` принимает запросы на уведомления. Запрос валидируется и публикуется как сообщение в **topic exchange** RabbitMQ.

2. **RabbitMQ**: Брокер сообщений, который разделяет API и воркеры уведомлений. Используется **topic exchange** (`events`) для маршрутизации сообщений по типу события (`event_type`), например, `user.email` или `user.sms`.

3. **Workers**: Независимые Python-скрипты, которые потребляют сообщения из конкретных очередей RabbitMQ:

   * **Email Worker**: Обрабатывает сообщения из `email_queue` (привязанной к ключу маршрутизации `user.email`) для отправки email.
   * **SMS Worker**: Обрабатывает сообщения из `sms_queue` (привязанной к ключу маршрутизации `user.sms`) для отправки SMS.
   * **Log Worker**: Подписан на все события (`#`) и логирует их, действуя как универсальный наблюдатель за уведомлениями.

4. **Механизм Retry & Dead Letter Queue (DLQ)**:

   * Если воркер не смог обработать сообщение (например, из-за временной ошибки внешнего сервиса), сообщение **не теряется**.
   * Вместо этого оно отправляется в специальный **retry exchange** (`events.retry`).
   * Сообщение попадает в **retry очередь** с TTL 30 секунд.
   * После истечения TTL сообщение автоматически возвращается в основную **events exchange** для повторной обработки оригинальным воркером.
   * Пользовательский заголовок `x-retry-count` отслеживает количество попыток. После **максимум 3 попыток** сообщение перемещается в **Dead Letter Exchange** (`events.dlx`) и соответствующую очередь DLQ (`email_dlq`, `sms_dlq`) для ручного анализа.

---

## 🛠 Начало работы

### Предварительные требования

* Docker
* Docker Compose

### Установка и запуск

1. Клонируйте репозиторий:

```bash
git clone https://github.com/suxds-repo/notification_service.git
cd notification_service
```

2. Постройте и запустите сервисы с помощью Docker Compose:

```bash
docker-compose up --build -d
```

Эта команда запустит следующие сервисы:

* `api` — FastAPI-приложение, доступное по адресу `http://localhost:8000`
* `rabbitmq` — брокер RabbitMQ с веб-UI на `http://localhost:15672` (логин: `guest`, пароль: `guest`)
* `email_worker`, `sms_worker`, `log_worker` — фоновые воркеры
* `rabbit-init` — разовая задача, которая создаёт необходимые exchanges и очереди RabbitMQ

Логи воркеров сохраняются в директорию `./logs` на хосте.

---

## 📤 Использование API

Отправляйте `POST` запрос на эндпоинт `/events` для публикации уведомления.

**Эндпоинт**: `POST /events`

### Примеры payload

#### Email уведомление

```json
{
  "event_type": "user.email",
  "user_id": 123,
  "email": "test@example.com",
  "message": "Hello from the notification service!"
}
```

#### SMS уведомление

```json
{
  "event_type": "user.sms",
  "user_id": 456,
  "phone": "+15551234567",
  "message": "Your verification code is 7890"
}
```

### Ответ при успешной публикации

Если событие успешно отправлено в RabbitMQ, вы получите подтверждение:

```json
{
  "status": "sent",
  "event": "user.email"
}
```

---

## 🧪 Тестирование логики Retry и DLQ

Воркеры могут имитировать ошибки для тестирования. Если поле `message` содержит слово `"fail"`, воркер выбросит исключение.

**Для теста:**

1. Отправьте запрос с `"fail"` в сообщении:

```bash
curl -X POST "http://localhost:8000/events" \
-H "Content-Type: application/json" \
-d '{
  "event_type": "user.email",
  "user_id": 999,
  "email": "fail@example.com",
  "message": "This message will fail"
}'
```

2. Просмотрите логи воркера. Например, для email воркера:

```bash
tail -f logs/email_worker.log
```

Вы увидите запись о первой попытке обработки, затем предупреждения о каждой последующей попытке retry (до 3 попыток).

3. После последней неудачной попытки сообщение попадёт в DLQ. Проверить это можно в RabbitMQ Management UI:

* Перейдите на `http://localhost:15672`
* Откройте вкладку **Queues**
* Вы увидите 1 сообщение в очереди `email_dlq`
